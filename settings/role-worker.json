{
  "$comment": [
    "Shared by planner, prover, builder, reviewer, and arbiter.",
    "",
    "Nothing here grants a write scope — that comes from ORCH_ROLE plus",
    "hooks/write-scope.sh, and for the builder from `isolation: worktree`,",
    "which the platform enforces harder than any settings glob. What this file",
    "does is deny every path to the base branch. Invariant 1 says exactly one",
    "candidate diff reaches it at a time, and the only role that gets there is the",
    "conductor, through the human gate.",
    "",
    "Launch:",
    "  ORCH_ROLE=builder CLAUDE_CODE_TASK_LIST_ID=orch-<repo> \\",
    "  claude --settings settings.json \\",
    "         --settings settings/role-worker.json"
  ],
  "permissions": {
    "allow": [
      "Read(**)",
      "Bash(orch:*)",
      "Bash(git status:*)",
      "Bash(git log:*)",
      "Bash(git diff:*)",
      "Bash(git add:*)",
      "Bash(git commit:*)"
    ],
    "deny": [
      "Bash(git merge:*)",
      "Bash(git push:*)",
      "Bash(gh pr create:*)",
      "Bash(gh pr merge:*)",
      "Bash(git rebase:*)",
      "Bash(git reset --hard:*)",
      "Bash(git filter-branch:*)",
      "Bash(rm -rf:*)"
    ]
  }
}
